[project]
name = "eval-loop"
version = "1.0.0"
description = "Evolution loop state machine"
requires-python = ">=3.10"
dependencies = ["pyyaml>=6.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
