{
  "name": "harness-cli",
  "version": "1.0.0",
  "description": "MAS Harness enforcement hooks (TypeScript components)",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^2.0.0",
    "@types/node": "^20.0.0"
  }
}
