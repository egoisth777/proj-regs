# IR-001: feat-001-add-task Implementation Record

## Feature
feat-001-add-task — Add Task functionality

## Date
2026-03-30

## Summary
Implemented the core Task data model (`Task` dataclass) and `TodoStore` class with `add_task` method in `artifacts/src/todo.py`. Added CLI `add` subcommand in `artifacts/src/cli.py` using argparse. The store persists tasks as JSON with auto-incrementing IDs, supports `TODO_FILE` environment variable override, and validates title (non-empty) and priority (low/medium/high).

## Files Modified
- `artifacts/src/__init__.py` — package init
- `artifacts/src/todo.py` — Task dataclass, TodoStore with `_load`, `_save`, `add_task`
- `artifacts/src/cli.py` — CLI entry point with `add` subcommand
- `artifacts/tests/conftest.py` — shared fixtures (`tmp_store`, `gapped_store`)
- `artifacts/tests/test_todo.py` — 9 unit test classes for add-task
- `artifacts/tests/test_cli.py` — 5 integration test classes for CLI add
- `artifacts/pyproject.toml` — project metadata and pytest configuration

## Key Decisions
- Used `dataclasses.dataclass` for the Task model for simplicity and serialization via `asdict`.
- JSON file with 2-space indent for human-readable persistence.
- Auto-incrementing ID derived from `max(existing IDs) + 1` to handle gaps gracefully.
