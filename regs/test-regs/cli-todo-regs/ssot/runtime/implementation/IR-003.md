# IR-003: feat-003-complete-task Implementation Record

## Feature
feat-003-complete-task — Complete Task functionality

## Date
2026-03-30

## Summary
Added `complete_task(task_id)` method to `TodoStore` in `artifacts/src/todo.py`. Implemented CLI `complete` subcommand in `artifacts/src/cli.py`. The method finds a task by ID, validates it exists and is not already completed, changes status to "completed", and persists.

## Files Modified
- `artifacts/src/todo.py` — added `complete_task` method
- `artifacts/src/cli.py` — added `complete` subcommand with `id` argument
- `artifacts/tests/test_todo.py` — 4 unit test classes for complete-task
- `artifacts/tests/test_cli.py` — 4 integration test classes for CLI complete

## Key Decisions
- Completing an already-completed task raises `ValueError` rather than being silently ignored, providing clear feedback.
- Linear scan for task lookup is acceptable given the expected small dataset size.
