# IR-002: feat-002-list-tasks Implementation Record

## Feature
feat-002-list-tasks — List Tasks functionality

## Date
2026-03-30

## Summary
Added `list_tasks(status_filter)` method to `TodoStore` in `artifacts/src/todo.py`. Implemented CLI `list` subcommand with `--status` filter (all/pending/completed) in `artifacts/src/cli.py`. Output format uses `[x]`/`[ ]` status markers with task ID, title, and priority.

## Files Modified
- `artifacts/src/todo.py` — added `list_tasks` method
- `artifacts/src/cli.py` — added `list` subcommand with `--status` argument
- `artifacts/tests/test_todo.py` — 7 unit test classes for list-tasks
- `artifacts/tests/test_cli.py` — 6 integration test classes for CLI list
- `artifacts/tests/conftest.py` — added `pre_populated_store` fixture

## Key Decisions
- Filter validation raises `ValueError` for invalid status values, consistent with add-task error handling.
- Empty list prints "No tasks found." and exits 0 (not an error condition).
